# YOLO 统一训练平台 — 代码修改文档

> 记录项目开发过程中所有实际修复的 bug 和新增的功能

---

## 1. Bug 修复

### 1.1 训练输出重复（每行显示两次）

**文件**: `gui/workers.py`

**问题**: 控制台每行输出都出现两次。

**根因**: `TrainWorker.run()` 中做了双重捕获：
- 替换 `sys.stdout` 为 `EmitCapturer`（LOGGER 的 StreamHandler 已通过 stdout 流向 capturer）
- 又向 LOGGER 添加了额外的 `LogHandler`（也写入同一个 capturer）

**修复**: 删除多余的 `LogHandler`，只依赖 `EmitCapturer` 捕获 stdout/stderr。

### 1.2 第二次训练 mAP/precision 图表空白

**文件**: `gui/workers.py`

**问题**: 第一次训练图表正常，从第二次训练开始 mAP 和 precision 图表空白。

**根因**: ultralytics LOGGER 的 `StreamHandler` 在模块加载时创建，存储了当时 `sys.stdout` 的对象引用。第一次训练时模块导入，StreamHandler 指向第一个 capturer。第二次训练创建新 capturer，但 LOGGER 的 handler 仍指向旧 capturer → 旧 worker 的 `_cur_epoch=10` → 去重跳过 → 验证数据不发射。

**修复**: 每次训练开始时，更新 LOGGER 的 StreamHandler 指向新 capturer：
```python
ultralytics_logger = logging.getLogger("ultralytics")
for h in ultralytics_logger.handlers:
    if isinstance(h, logging.StreamHandler) and hasattr(h, 'stream'):
        h.stream = capturer
```

### 1.3 训练输出信号洪水导致栈溢出

**文件**: `gui/workers.py`

**问题**: 训练过程中进程崩溃，退出码 `0xC00000FD`（Windows 栈溢出）。

**根因**: TQDM 进度条每秒用 `\r` 更新几十次，每次触发 `_parse_metrics()` 匹配训练行后发射 `epoch_done` 信号，信号洪水导致 Qt 事件队列溢出。同时在 `_parse_metrics` 中使用 `print()` 调试导致无限递归（stdout 被捕获 → 触发解析 → 再次 print → 递归）。

**修复**:
- 训练 loss 只在值变化时发射（去重）
- 验证指标用 `_last_val_epoch` 追踪，每 epoch 只发射一次
- 禁止在 `_parse_metrics` 中使用 `print()`

### 1.4 Force Stop 按钮无法及时终止训练

**文件**: `gui/workers.py` + `gui/main_window.py`

**问题**: 点击 Stop 后训练继续运行，要等到当前 epoch 结束才生效。

**根因**: 原来的 `on_train_epoch_start` 回调只在每个 epoch 开始时检查 stop 标志。

**修复**:
- 删除 Pause 按钮，Stop 改为 Force Stop
- 添加 `on_train_batch_end` 回调，在每个 batch 结束后检查 stop 标志
- `force_stop()` 方法替代 `pause()` / `resume()` / `stop()`

### 1.5 训练完成后 GPU 内存未释放

**文件**: `gui/workers.py` + `gui/main_window.py`

**问题**: 训练完成后 `nvidia-smi` 显示显存占用与训练时相同。

**根因**: `model`（YOLO 对象）和 `result`（训练结果）在线程局部变量中被引用。YOLO 内部存在循环引用（model ↔ trainer），普通引用计数无法回收。

**修复**: 训练结束后显式清理：
```python
del model, result
import gc
gc.collect()                    # 强制回收循环引用
if torch.cuda.is_available():
    torch.cuda.empty_cache()    # 让 CUDA 释放未使用的显存块
```

同时在主线程的 `_on_train_done()` / `_on_train_error()` 中也添加清理作为双重保障。

### 1.6 第二次训练状态未重置

**文件**: `gui/workers.py`

**问题**: 第二次训练时 `_cur_epoch` 保持上一次训练的最终值。

**根因**: `run()` 结束时 `_cur_epoch` 未重置。

**修复**: 在训练完成后（`model.train()` 返回后）重置：
```python
self._cur_epoch = None
self._last_val_epoch = -1
```

---

## 2. 功能新增

### 2.1 训练页面

| # | 功能 | 说明 |
|---|------|------|
| 1 | 视频检测 | `VideoDetectWorker` 逐帧检测，支持暂停/停止/帧率同步 |
| 2 | 实时图表 | 6 个 matplotlib 图表（box_loss/cls_loss/dfl_loss/lr/mAP/precision） |
| 3 | 进度信息 | epoch 进度条 + 已用时间 + ETA + GPU 显存 |
| 4 | Force Stop | 单一停止按钮 + `on_train_batch_end` 回调，batch 级别响应 |
| 5 | Optimizer 选择 | 下拉框：auto/SGD/Adam/AdamW/NAdam/RAdam/RMSProp |
| 6 | Cache Images | 勾选框，缓存图片加速训练 |
| 7 | Resume Training | 勾选框，从上次 checkpoint 继续训练 |
| 8 | cfg 文件 | Browse 按钮，训练配置 YAML（覆盖所有参数） |
| 9 | hyp 文件 | Browse 按钮，超参数 YAML（lr/momentum/augment 等） |
| 10 | Loss 更新模式 | `"epoch"` 平滑 / `"batch"` 实时，可在 `gui/settings.py` 切换 |

### 2.2 导出页面

| # | 功能 | 说明 |
|---|------|------|
| 1 | ONNX Opset | 自定义 opset 版本（7-20），默认 17 |
| 2 | Dynamic Batch | 启用动态 batch 维度 |
| 3 | Simplify ONNX | 用 onnx-simplifier 简化模型，默认开启 |
| 4 | 导出详情 | 输出 input/output shape、文件大小、ONNX opset 版本 |

### 2.3 配置文件

所有可调参数集中在 `gui/settings.py`，修改后重启生效：

```python
# gui/settings.py

# Loss 图表更新模式:
#   "epoch" — 每个 epoch 完成后画一个点（平滑，不抖动）
#   "batch" — 每个 batch 结束都更新当前点（实时，会抖动）
LOSS_UPDATE_MODE = "batch"

# 视频检测帧率模式:
#   True  — 和原视频帧率对齐（播放速度 = 原视频速度）
#   False — 识别多快就多快（最快速度，不等待）
VIDEO_SYNC_FPS = True
```

### 2.4 其他

| # | 功能 | 说明 |
|---|------|------|
| 1 | 自定义模型选择 | Browse .pt + 下拉框双模式 |
| 2 | Dataset YAML 选择 | 下拉框 + Refresh + Browse 按钮 |

---

## 3. 技术实现

### 3.1 训练输出捕获

```
ultralytics 训练输出
    ↓
sys.stdout (EmitCapturer)
    ↓ _parse_metrics()
训练 loss: 值变化时即时 emit（TQDM \r 去重）
验证 mAP:  每 epoch 去重 emit（_last_val_epoch）
学习率:    on_train_epoch_start 回调
    ↓
epoch_done 信号 → GUI 图表更新
```

- LOGGER 的 StreamHandler 在每次训练开始时更新指向新的 capturer
- `_parse_metrics` 中禁止 `print()`（stdout 被捕获会无限递归）

### 3.2 图表更新机制

```
_train_pending_metrics (缓冲队列)
        ↓ QTimer 500ms
_flush_charts() (主线程)
        ↓
_apply_metrics() → 更新 6 个 matplotlib 图表
_update_time_display() → 更新时间显示
```

### 3.3 Force Stop 流程

```
Force Stop 按钮
    → self.stop_flag = True
    → on_train_batch_end 回调检查
    → trainer.stop = True
    → 当前 batch 完成后 break
    → validate() + save_model()
    → final_eval() + strip_optimizer()
    → del model + gc.collect() + cuda.empty_cache()
    → 正常退出
```

不是杀进程，而是设置标志让训练循环自己走完清理流程。模型文件完整且已压缩（optimizer state 被移除）。

### 3.4 GPU 内存管理

训练结束后在 worker 线程中：
```python
del model, result          # 删除 Python 引用
gc.collect()               # 强制垃圾回收，释放循环引用
torch.cuda.empty_cache()   # 让 CUDA 释放缓存的显存块
```

主线程 `_on_train_done()` / `_on_train_error()` 中也执行清理作为双重保障。

### 3.5 close_mosaic

`close_mosaic`（默认 10）控制最后 N 个 epoch 关闭 Mosaic 数据增强。关闭时会重建 dataloader，暂停几秒属正常现象。

| 场景 | close_mosaic | 说明 |
|------|-------------|------|
| 默认 | 10 | 推荐，最后 10 epoch 关 Mosaic |
| 小数据集 | 5 | 早点关让模型收敛 |
| 大数据集 | 15~20 | 多练复杂场景 |
| 关闭 | 0 | 全程 Mosaic，一般不建议 |

### 3.6 配置文件

所有可调参数集中在 `gui/settings.py`，`main_window.py` 通过 `from . import settings` 导入使用。

---

## 4. 模块状态

### 已在 GUI 中集成

| 功能 | Tab | 状态 |
|------|-----|------|
| 图片/视频检测 | Detect | ✅ |
| 模型训练（实时图表） | Train | ✅ |
| 模型导出 | Export | ✅ |
| 速度测速 | Benchmark | ✅ |

### 已实现但未集成到 GUI

| 模块 | 路径 | 状态 |
|------|------|------|
| 格式转换 | `data/converter.py` | CLI 可调用 |
| 数据清洗 | `data/cleaner.py` | CLI 可调用 |
| 数据集划分 | `data/splitter.py` | CLI 可调用 |
| 自定义损失函数 | `losses/` | 独立可用 |
| 模型剪枝 | `pruning/pruner.py` | 独立可用 |
| 模型量化 | `pruning/quantizer.py` | 独立可用 |
| ONNX 部署 | `deployment/onnx_deployer.py` | 独立可用 |
| TensorRT 部署 | `deployment/tensorrt_deployer.py` | 独立可用 |
| 日志分析 | `analysis/log_analyzer.py` | CLI 可调用 |
| Web UI | `web/app.py` | 独立可用 |
| CLI | `tools/cli.py` | 独立可用 |

### 核心框架（未被 GUI 使用）

| 模块 | 说明 |
|------|------|
| `core/registry.py` | 9 个全局注册表 |
| `core/config.py` | YAML 配置管理 |
| `core/model_factory.py` | 模型工厂模式 |
| `core/base_model.py` | 抽象基类 |
| `core/models/*.py` | 5 个模型包装器 |

> GUI 直接调用 `ultralytics.YOLO`，未使用 `ModelFactory`/`BaseYOLOModel` 抽象层。

---

## 5. 已知问题与 TODO

| # | 问题 | 优先级 | 说明 |
|---|------|--------|------|
| 1 | 硬编码路径 | 高 | `coco128.yaml` 路径写死在代码中 |
| 2 | core/ 未使用 | 中 | `ModelFactory`/`BaseYOLOModel` 未被 GUI 使用 |
| 3 | losses 未集成 | 中 | 自定义损失函数是独立实现的 |
| 4 | pruning 未集成到 GUI | 低 | 剪枝/量化工具独立可用 |
| 5 | TensorRT 硬编码 | 低 | 输出形状写死为 YOLOv8 格式 |
| 6 | 无单元测试 | 低 | `tests/` 目录为空 |

---

## 6. 环境要求

- Python 3.9+
- PySide6
- torch >= 2.0.0
- ultralytics >= 8.1.0（本地副本 `ultralytics/`）
- matplotlib（训练图表）
- opencv-python, numpy, pyyaml, tqdm
- onnx（可选，导出时显示 input/output shape）

---

## 7. 相关文档

- [Bug 修复日志](docs/bugfix-log.md) — 详细的问题排查过程、根因分析和修复方案
